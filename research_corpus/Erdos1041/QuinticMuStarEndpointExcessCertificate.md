# Quintic `mu_*` endpoint excess certificate

## Status

**EXACT ENDPOINT THEOREM.**  Put

```text
mu_* = (-70+10 i sqrt(5))/27.
```

At this gap-zero parameter the allowed phase fibre is a half-circle.  Let
`E_+` and `E_-` be the paired arm excesses at its two endpoint rays, with the
normalization below.  Then

```text
E_+ < -47/125 < -3/8,
E_- <  -2/5  < -3/8.                              (M0)
```

Complex conjugation gives the same two bounds at the conjugate value of
`mu_*`.  This is an ordinary mathematical proof backed by an exact checker:
the checker uses algebraic-number Sturm counts, rational Krawczyk boxes, and
rational interval inequalities.  Floating point has no acceptance role.

The strict `-3/8` endpoint margin is the input needed by the separate angular
subharmonic interpolation argument.  This note certifies the two endpoint
rays; by itself it does not assert the interpolation theorem, the ordinary
double-tight carrier, `EX-W`, `EX-U`, the model-to-real transfer, degree five,
or unrestricted Erdos 1041.

Run

```bash
python3 research_corpus/Erdos1041/scripts/check_erdos1041_quintic_mustar_endpoint_excess.py
```

## 1. Exact exceptional-fibre normal form

Write `a=sqrt(5)` and

```text
r=-(5+ia)/3,             s=-(5-2ia)/3.
```

For `mu=mu_*`, the quintic model factors as

```text
F_mu(x)=x^2(x-r)^2(x-s).
```

The change of variable `x=r(u+1)/2` gives

```text
F_mu(x)=r^5 P(u)/32,
P(u)=(u^2-1)^2(u+ia).                              (M1)
```

Thus the two endpoint rays are `P(u)=sigma T`, `T>0`, for
`sigma=+1,-1`.  The discriminant in the target value `v` is

```text
disc_u(P(u)-v)
 =v^2(3125v^2-14400ia v-55296).                   (M2)
```

Its nonzero zeros are not real.  Consequently every inverse root on either
open real ray is simple, and the five inverse branches cannot collide.

The two branches issuing from the double zero `u=-1` are selected without a
numerical continuation convention.  Indeed

```text
Im P(p+iq)|_{q=0}=a(p^2-1)^2,
Im P(p+iq)|_{p=0}=(q+a)(q^2+1)^2.                 (M3)
```

Hence an open nonzero real-ray branch cannot cross the real or imaginary
axis.  The only additional separator needed for the negative easy arm is
`q=-a`: there `E=-4ap^2(p^2-6)`, and at the negative-`p` crossing one has
`Re P(-sqrt(6)-ia)=120sqrt(6)>0`, the wrong endpoint ray.  Thus the signs,
and for the negative easy arm the strip `-a<q<0`, identify the terminal
fifth-root sector.  The four selected sectors are

```text
sigma=+1: zeta=e^(4pi i/5)  (easy), e^(6pi i/5)  (hard),
sigma=-1: zeta=-1           (easy), e^(3pi i/5)  (hard),
```

where `zeta^5=sigma`.  The local expansion

```text
P(-1+delta)=4(-1+ia)delta^2+O(delta^3)
```

then binds all four terminal sectors to the intended double-hub arms.

## 2. Terminal support coordinates

Set

```text
rho=(-5-ia)/sqrt(30),        d=rho zeta,
Z=conj(d)x=(sqrt(30)/6)conj(zeta)(u+1)=W+iV.       (M4)
```

For one selected arm the standard support--oscillation estimate is

```text
e_arm <= -Re(d)+B,
B=integral h |dV|,
h=tan(|theta|/2),                                      (M5)
```

where `theta` is the tangent angle from the terminal direction `d`.

For `u=p+iq`, write `P'(u)=D+iI`.  Direct expansion gives

```text
E(p,q):=Im P(u)
 =a(p^4-6p^2q^2-2p^2+q^4+2q^2+1)
  +5p^4q-10p^2q^3-6p^2q+q^5+2q^3+q,

D=5p^4-30p^2q^2-6p^2+5q^4+6q^2+1
  -12ap^2q+4aq^3+4aq,

I=4p(ap^2-3aq^2-a+5p^2q-5q^3-3q).               (M6)
```

If `zeta=c+is`, define

```text
A=sigma(cD-sI),          B_0=-sigma(cI+sD).        (M7)
```

Along `P(u)=sigma T`,

```text
dW/dT=(sqrt(30)/6) A/|P'(u)|^2,
dV/dT=(sqrt(30)/6) B_0/|P'(u)|^2,
h=|B_0|/(sqrt(A^2+B_0^2)+A).                      (M8)
```

Therefore `A>0` implies the deliberately coarse but very effective bound
`h<1`.  Only the number and height of the turns of `V` remain.

## 3. Exact resultant atlas

For each of the four branches, the checker eliminates `p` from

```text
E(p,q)=0, A(p,q)=0
```

over the exact algebraic field generated by the relevant radicals.  It uses
Sturm counts with algebraic coefficients on the branch's open `q`-half-axis.
Every nonzero real resultant ordinate is isolated in a rational interval;
the multiplicity of that ordinate equals the number of rational Krawczyk
boxes certified above it.  The exact signs of `p,q,Re P` put every such box
outside the selected tract.  Since `A` is positive in the terminal expansion,
this proves

```text
A>0 on all four selected open arms.                (M9)
```

The same computation with `B_0` proves:

- the positive easy, negative hard, and negative easy arms have no finite
  `V`-turn;
- the positive hard arm has exactly one finite `V`-turn.

For the negative easy arm the last assertion is especially transparent:
`B_0=-I`, and the exact resultant factors as

```text
2560000 q^4(q+a/5)^2(q+a)(q^2+1)^2
 * (q^4+6a q^3/5+3q^2+4a q/5+6/5)^2.             (M10)
```

Here `q=-a` is the zero `P(-ia)=0`, while `q=-a/5` is only the terminal
degree drop: direct exact gcd at `q=-a/5` is `1`, so it is not a finite turn.

For the unique positive-hard turn the checker isolates

```text
-2725774/10^6 < p < -2725769/10^6,
-2437249/10^6 < q < -2437245/10^6,                (M11)
```

and rational interval evaluation of (M4) gives

```text
0.873971781... < V_max < 0.873977419... < 7/8.    (M12)
```

This is the only rational event box needed; the earlier multi-threshold
partition is unnecessary.

## 4. Endpoint variation budgets

The inverse expansion in a terminal fifth-root coordinate is

```text
u=y-ia/5+(8ia/25)y^(-2)+(12/25)y^(-3)
    -(144ia/625)y^(-4)+O(y^(-6)).                 (M13)
```

It gives the exact terminal transverse coordinate

```text
V_infty=(sqrt(30)/6)(-s-ca/5).                    (M14)
```

The checker proves the outward rational bounds

```text
positive hard:  V_infty > 433/500,
positive easy:  |V_infty| < 21/100,
negative hard:  |V_infty| < 3/4,
negative easy:  |V_infty| < 5/12.                (M15)
```

All arms start with `V=0`.  By (M9), `h<1`; by the exact turn count, the
positive-hard total variation is at most `2V_max-V_infty`, while the other
three total variations are their endpoint variations.  Hence

```text
B_+ < 2(7/8)-433/500+21/100 = 547/500,
B_- < 3/4+5/12                 = 7/6.             (M16)
```

Finally, direct radical simplification and exact rational comparison give

```text
sum_{sigma=+1 arms} Re(d)
 =sqrt(30)(1+sqrt(5))/12 > 147/100,

sum_{sigma=-1 arms} Re(d)
 =[5(sqrt(5)-1)+sqrt(5)sqrt(10+2sqrt(5))]/(4sqrt(30))
 >47/30.                                               (M17)
```

Combining (M5), (M16), and (M17) proves

```text
E_+ < 547/500-147/100 = -47/125,
E_- < 7/6-47/30       = -2/5,
```

which is (M0).

## 5. Verification and claim boundary

The checker proves exactly:

1. the factorization (M1), the real-ray collision exclusion (M2), and the
   sign-based tract assignment (M3);
2. complete algebraic resultant counts for `A=0` and `B_0=0` on the four
   selected tract half-axes, with rational Krawczyk boxes accounting for every
   resultant multiplicity;
3. the unique turn box (M11), the rational transverse bounds (M12), (M15),
   and the radical projection inequalities (M17);
4. the endpoint pair inequalities (M0).

The checker does not numerically integrate an arm, and its decimal output is
only a projection of rational intervals.  The support--oscillation lemma and
the elementary inverse-branch continuation argument are ordinary mathematics;
the checker supplies their exact finite algebra and inequalities.  No claim is
made here about the interior of the half-circle without the separate angular
interpolation theorem.
