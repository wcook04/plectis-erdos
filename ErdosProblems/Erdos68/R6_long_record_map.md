# Long-record destinations and lag

The frozen long record is not line-edited. Locations below refer to the authored parts included in this packet. The disposition says the mutable authored-part tree is absent; in that working tree, append the corresponding material to the live `ErdosProblems/Erdos68/TailIdealCertificate.md` or `DivisorChannelBasis.md` until the long record is reassembled.

## Material removed from the short note

At `sec:open`, retain `eq:canonical-open-target`, `eq:signed-block-gap` and the reference to `eq:joint-loss-budget`. Move the following exact discussions together, preserving their hypotheses:

- **“Prime powers and real gaps”**: to `reasoning-parts/erdos68/extended_record.tex`, `sec:ext-plateau`. Keep the exact reduced-gap threshold and distinguish the model fractions `1/q^e` from an example realised by the factorial orbit. The relevant live sources are `PrimeThresholdParity.lean` and `FactorialZeroPlateau.lean` and the precise modules named by the frozen route; do not infer a cofinal producer from a finite valuation record.
- **“Carry values and predecessor residues”**: to the same `sec:ext-plateau`, immediately following the lower-endpoint discussion. Keep both doubled-prime branches, including the predecessor congruence. Neither branch is a statement merely about the distribution of carry values.
- **“Information lost by a window or a fixed owner”**: to `sec:ext-nogo`, with a cross-reference to `sec:ext-collision` for the fixed-owner arithmetic. The universal adjacent-window inequality falsifies that test only; it does not eliminate restrictions on the actual orbit.

The replacement short-note paragraph gives the shortest-support failure with its proof. The sharp radius constant already present under `res:radius-constant` remains an existing result, not a new round-6 claim.

## New material after mathematical adjudication

Place the finite-source closure theorem, unordered-block proof, quadratic cutoff and cancellation-preserving algorithm next to the existing lcm-envelope certificate in the live `TailIdealCertificate.md`. Preserve the old envelope: it still supports the p-adic argument and the simpler stopping test. The new theorem replaces the old *leading explanation*, not the truth of the old theorem.

Keep the depth-24 certificate beside its ordinary proof: scalar prefix through 32, net-source checks at 36 and 48, with exact residues. Link the supplied executable and its certificate file. Do not call the separate diagnostic through 1000 a premise of the shorter certificate.

Place the remote projection determinant and finite-rank support lattice after the scalar support equation in `DivisorChannelBasis.md`. Its two parameters are cancellation depth `D` and lower support `r`; they are distinct from the maximum support. Record the sharp small example `mu_(2,4)=360` with both the dual congruence and attaining vector. Do not replace the remote-grid result: the constructions optimise different properties.

## Actual lag in the supplied published assembly

**`sec:ext-certificates`** still states that integer kernels on the stated support have moment `L_D`. This is the previously withdrawn support-domain claim. The current short note and live disposition already corrected it; this is assembly lag, not a new defect in the live note. Remove the unsupported arrays and bounds or supply the arrays with an explicitly different auxiliary domain before restoring them.

**`res:split-factor-normalized-collision`** in the assembled long record still requires semantic reconciliation with the correctly normalised theorem. The presence of a label alone does not identify its mathematical statement. The round-6 live global criterion is the authority for the displayed comparison. Do not resurrect the missing-factorial version.

**`sec:ext-certificates` carry paragraphs** combine external interval evidence and local regenerated evidence. Reconcile exact source and receipt histories at one immutable release before presenting them as a replay of the same current object. Neither old numerical run was repeated here.

**Current/pinned source boundary:** the live zip is authoritative at packet checkpoint `e9a122bf7cf9ff7a20e45a6215876b3825aeee18`; the short note retains public pin `32545d77c4b5cfd72ebd36c8dcd2418bf7cc4d31`. The pinned sibling #269 source was fetched successfully. No claim that the public pin contains every newer live #68 addition is made.

## Selective adversarial findings

The two finite exclusions and the first companion theorem **survive the local mathematical check**; their large numerical receipts remain inherited. The quadratic cutoff **survives**, with the prime witness, integer quotient and strong-induction steps explicit. The remote lattice theorem **survives**; replacing its integer lattice by a rational span would break the conclusion, because the determinant need not be a unit. The universal claim `g_D = gcd(u_(D+1),...,u_(2D))` has a **gap at the all-depth source-cancellation step**: exact certificates were produced for depths 2 through 60, not for every depth. A finite gcd plateau without a closure certificate **fails as a proof method**. The claim that a solved coefficient lattice supplies residual nonintegrality **fails at the residual identity**: zero-moment corrections are integer translations.
