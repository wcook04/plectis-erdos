# Long-record lag and the surviving frontier

This is a destination and reconciliation note, not a line edit of the co-primary long record. Line numbers refer only to this supplied `02_long_record.tex`; labels are the durable handles.

## Mathematical status that needs reconciliation

**Critical-value budget.** At `res:critical-value-budget` and `eq:critical-value-power-budget`, under “A critical-value budget through degree five” (around line 1658), the limited theorem is still true but no longer the frontier. The abstract (around line 78), introduction (107) and table (238) inherit the same lag. `FreePointQuadraticAllDegrees.md` proves the weighted all-degree quadratic inequality and its critical-value consequence. Retain any finite proofs as subsidiary examples, not as the current coverage limit. The rejected floating cover remains rejected; it is not the proof superseding these entries.

**Fixed-degree closure.** `prob:perturb1041`, “two-stage generic perturbation with slack” (around line 2255), is a possible stronger forward-stability problem, not a necessary remaining condition for existence. The paragraph around line 2282 and the earlier “Coefficient perturbation and slack stability” table row should distinguish this from `GenericSufficiencyClosure.md`. Lower semicontinuity of the all-curve functional already transfers a bound from a dense generic subclass at fixed degree. It does not assert a uniform collar or quantitative forward transport of a selected curve.

**Generic topology.** The historical compact-ray and saddle-block discussion should be read beside `AttachmentAwareReeb.md`. The ordinary generic slit-sheet construction exists; the sharp metric selection/gluing conclusion is separate. Do not turn a remaining formalisation task into an assertion that the ordinary construction is missing. Do not reuse the historically disputed local-valence argument.

**Threshold-free comparison.** The live 13/25 authority `AngularBudgetLowCriticalClosure.md`, Corollary B'', already records its scale-free implication. Any summary calling 71/10 the strongest threshold-free numerical result is behind that authority. Preserve 71/10 for its independently inspectable area proof and preserve its original containment statement.

**Perimeter witness.** The discussion after the one-root gamma-bound counterexample must use `z^N-a z` with fixed `a>1`, then take `N` large and `a` down to one. At level one, `z^N-z` does not have the claimed isolated one-root component. The corrected limiting lower bound is a necessary constant of at least `2 pi`, not a proved upper bound. The live note correction is keyed to `res:one-root-gamma-false`; the underlying authority is `ExactObstructionsR2.md`.

**Authored parts and generated routes.** The `reasoning-parts/erdos1041/family_catalogue.tex` section `sec:erdos-1041-complete-family-map` should be regenerated only after these supersession relations are reconciled. The ordinary/Lean distinctions in the authored `extended_record.tex` must survive. The frozen `corpus_route.json` and mandate still list FP_m for m>=5, generic slit construction, and full forward-slack perturbation as open obligations. Those entries are not reliable current mathematical frontiers merely because they are generated. Preserve genuine formalisation tasks as such.

## Destinations of short-note cuts

The complete cut text is in `edits/long_record_transfers.md`.

The arity/capacity conclusions and the minimal-hub persistence window belong after the general constant-factor construction in the long record, under component-sensitive refinements and actual two-root persistence. Their target-length closures with mu<=1/2 are numerically subsumed by mu<=13/25, but their component geometry can still be informative.

The removed “Statements and declarations” material and full old source guide belong in the long-record verification appendix, retaining exact ordinary/Lean boundaries, duplicate-interface distinctions and immutable locations. The Dubinin narrative belongs in the adjacent-literature discussion, retaining the stated printed-page reference and hypothesis distinction.

## What remains, stated as mathematics

For the compact fixed-degree class of monic polynomials with roots in the closed unit disc, define Lambda as the infimum of lengths of curves in the closed unit lemniscate joining different listed root occurrences. The target is Lambda(f)<=2. Dense-generic sufficiency is already an ordinary result; rescaling an enclosing disc of radius R<1 yields strict containment and length for the open-disc problem.

The minimum admissible canonical critical-arc length is an upper bound for Lambda, not a known equivalent functional. The critical-value restriction |f(c)|<=1 must accompany that route. The missing sufficient estimate is a jointly admissible short connection, not merely a small critical value, a good angular mean, or a generic topological decomposition.

The new sector theorem supplies a different competitor on an explicit family. The new cusp law proves singular metric sensitivity there, but neither result solves the universal selection problem.
