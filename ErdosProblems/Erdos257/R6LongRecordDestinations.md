# r6 labelled destinations (`long_record_lag.md`)

The published long-record assembly is not edited in this wave. Frozen R
`public-source-redacted://release-final-20260905` is not
mutated. Labels below are live problem-tree homes for a later long-record
editor.

## Support-criteria sequence lag

`a257_front.tex` `sec:257-weighted-returns` (around the paragraph “Other
support criteria sharing the small-shift endgame”) and
`a257_family_catalogue.tex` still lead with
\(\sum_j C_j 2^{j\alpha_j} B_j/(B_j-1)^2<\infty\).

The live short note now carries the stronger one-inverse-power cover, the
first-logarithmic-moment cost, the cube cost, and the finite mixed theorem.
After the r6 editorial apply, the finite kernel sits at
`sec:common-kernel` / `eq:mixed-finite-kernel` immediately before the
positive-cover theorem.

Suggested long-record order, with live homes:

| Label | Live home |
|---|---|
| `lem:257-common-kernel` | short note `sec:common-kernel`; research kernel in `MixedSupportSynchronisation.md` |
| `thm:257-one-inverse-cover` | short note positive-cover theorem |
| `prop:257-first-log-cost` | `CoverFirstLogarithmicMoment.md` |
| `thm:257-finite-mixing` | short note `res:mixed-supports` |
| `prop:257-no-growing-zero-windows` | `NoGrowingZeroWindows.md`; proof in `ElementarySupportConstraints.md` |

Keep the older cover formula only to state the proved strict separation.

Do not rename or displace the all-base flagship `res:reciprocal-support`.

## New countable theorems (ordinary; not spliced into the short note)

Complete proofs: `CountableGluingAndActualBarriers.md`. Compact insert
(companion only): `countable_gluing_insert.tex`.

| Label | Statement in the research note |
|---|---|
| `lem:257-tail-stability` | Lemma 2.1 |
| `thm:257-delayed-gluing` | Theorem 2.3 |
| `prop:257-log-erasure` | Lemma 3.1 |
| `thm:257-diagonal-outside-mixed` | Theorem 4.3 |
| `cor:257-prime-power-augmentation` | Corollary 4.4 |
| `thm:257-prime-partition-no-sync` | Theorem 5.3 |
| `prop:257-hereditary-residual` | Proposition 6.1, space \(2^S\) |

Theorem 2.3 answers a constructed-host countable-gluing question. It does
not assert closure for an untruncated union. State that distinction before
the strict example.

## Short-note cuts already have long-record homes

Boolean–Möbius: `a257_front.tex` `sec:257-exact-descriptions` `thm:bmc`.
General real-target repair: `thm:generic-repair`. Geometry:
`thm:geometry`, `thm:supported-dichotomy`. Six-row finite-denominator
table: illustration beside a new `thm:257-signed-finite-period`, not the
unrelated `thm:signed-periodic`. Tower-scale mass: analytic supplement
after the quantitative bound; the \(H_A=o(\ell)\) corollary remains in the
short note.
