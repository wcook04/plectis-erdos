# Erdős #251 Type B r6 long-record destinations

Companion landing of the destinations in `02_long_record_changes.md`.
The frozen R checkout `public-source-redacted://release-final-20260905`
is not mutated. The round-6 short note and Lean slice govern current statements.
Labels below are authored labels from the supplied long-record files, not a
guessed public branch.

## `reasoning-parts/erdos251/extended_record.tex`

### `sec:xr-divisorhit` (supplied lines 93–106)

Replacement heading: **A divisor-hitting formulation**.

Replacement paragraph after `xr:divisor-hit`:

> The divisor-hitting condition is equivalent to irrationality within an
> integer-digit dyadic recurrence. Irrationality gives nonintegrality of every
> positive shift. Conversely, a rational initial value gives one eventually
> integral positive shift; the cocycle identity makes all its positive
> multiples eventually integral as well. Applying the proposed condition to
> that shift gives a contradiction. The quantifier organisation may be useful
> for a producer, but the equivalence alone is not an analytic saving.

The live short note already contains the relevant classifier. This is not a
new theorem of the r6 return.

### `sec:xr-compression` (supplied lines 114–123)

Retain the valid conditional pigeonhole observation. Replacement of the
finite-state real-tail claim:

> After the rational transient, the tails lie in one fixed lattice. A dyadic
> first-moment bound gives a positive proportion of indices with
> `T_N ≤ C_1 log X`, for a fixed sufficiently large `C_1`. On that restricted
> set only `O_d(log X)` lattice values occur, so one value is repeated
> `≫_d X/log X` times. The bound on the number of states grows with the scale.
> Fractional parts are eventually periodic; the full real tails need not have
> a fixed finite state space. These repeated values give integral differences,
> whereas the desired producer needs a nonintegral difference.

Do not delete the bounded-scale counting argument merely because rationality
alone does not force repeated real tails.

## `02_long_record.tex`

### Free-pair discussion (supplied lines 661–669)

Use the same scale-dependent qualification. The free-pair and fixed-shift
conditions are alternative exact criteria after all their quantifiers are
imposed. A single nonintegral pair is not the full free-pair criterion.

### Paragraph following `prob:smallpair` (supplied lines 1317–1330)

The sentence about density-zero events being produced on a sparse set is
already compatible with sparse averaging. Do not label it as a newly
discovered error. Narrow the broad claim about fixed patterns to:

> The cited isolated-gap and fixed-cluster statements do not themselves give
> the residue-sensitive weighted continuation estimate used here. A future
> argument can combine a sparse configuration count with a global tail budget;
> density zero alone does not rule out that route.

## `family_catalogue.tex` — Actual real prime gap tail (supplied lines 71–90)

This entry already records the actual bridge and its conditional endpoint.
Leave the mathematical scope intact. A general handoff obligation still
requesting that bridge is stale metadata, not a missing theorem.

## New ordinary research appendix

`SharpSparseFeedback.md` sits after `SparseRationalisation.md` as a separate
ordinary result. Keep the old paired construction: it works below an arbitrary
divergent envelope and remains a transparent constraint-separation example.
The r6 theorem optimises the regular polylogarithmic regime and adds the exact
dimension law. It does not replace every part of the old theorem, and it is
not a displayed statement of the registered flagship.

The principal failure map is in `00_IDEA.md`. Preserve the old
bounded-alphabet sparse obstruction, the adaptive-support entropy argument,
the polynomial complete-tail example, the sparse irrational phase-concentration
example, and the failed affine escape identity.

## Generated metadata rather than mathematical source

`03_research_packet.json` is not hand-edited. Parent `apply_assimilation_rows.py`
on `assimilation_spec.json` is the packet route. Finite observations stay
observations. The corrected finite-continued-fraction boundary (finite data
do not determine an irrationality exponent) is not undone.

No new numerical prime computation or denominator exclusion was attempted in
this return.
