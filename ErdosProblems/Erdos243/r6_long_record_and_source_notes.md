# Companion reconciliation and formal-source boundaries

This is a label-based repair and destination report. The frozen `02_long_record.tex` has not been edited. Its repeated old assertions are not new defects in the repaired live short note or the current ordinary proof supplements.

## 1. Findings that remain live

The bounded-negative flagship survives. Its published-pin eventual form was read at `ReciprocalTailRigidity.lean:2352`, commit `32545d77c4b5cfd72ebd36c8dcd2418bf7cc4d31`. The copied current source places the corresponding declaration at line 2272. This is a version difference, not a licence to change a public line link to the ZIP's coordinate without changing its commit.

The new scalar no-vanishing theorem exists in the ZIP at `SparseResetRecovery.lean:612`. Fetching that older public file in full, then searching it for `eventually_zero_of_summable_negativeRelativeMass_scalar`, returned no match. A request for lines 595–630 returned no content. The manuscript's stronger scalar theorem has a complete elementary proof, and the packet records its live formal development; this review does not dispute it. The public pin nevertheless cannot serve as the claimed pointer to that newer declaration. The proposed preview attaches its pin-specific assurance only to the verified bounded-negative declaration. Release the scalar source at a suitable immutable checkpoint before restoring a public scalar link.

The `verification/comparator.json` at the note's pin was read. It contains selected bounded-negative, scalar-interface and sibling declarations. Reading the configuration is not running Comparator and does not establish that newer candidate statements have been independently compared. No run was made here.

The coefficient bounded-height supplement is available at the pin and explicitly records its r5 assimilation. The ordinary cubic Section 8 was also fetched and contains the corrected density/phase limitations. These are existing results, not r6 discoveries.

## 2. A remaining-obligation label that is too broad

The supplied handoff calls “CRT supply of the covering list” outstanding. The live `LcmRecordCrossing.lean` contains

```text
ErdosProblems.Erdos243.LcmRecordCrossing.exists_crt_covering_progression
```

Its input is a finite family `m : Fin B -> Nat` with `m_i>B` and pairwise coprimality. Its conclusion supplies an actual CRT start in the product interval and all translated coverings for every later clearance divisible by the family. Thus the finite CRT construction is already written in the supplied source.

What is separate is producing the finite old family from an infinite orbit, composing that family with the coefficient fence, and formalising the infinite weighted/analytic arguments. The candidate `bounded_of_old_coprime_moduli` composes the existing CRT construction with `CoefficientDivisorFence.stays_below_fence`, retaining the old-family hypothesis explicitly. It does not assume boundedness, a global weighted theorem, or the #243 endpoint as an input.

Neither the new file nor its imports were compiled in this review. The supplied comments and deferred build history must not be turned into a fresh successful receipt. The source layout is enough to avoid inventing another finite CRT interface with the same purpose; a focused build remains necessary.

Similarly, `derive_the_two_analytic_hypotheses` is an obsolete description of an ordinary mathematical gap. The canonical tail estimate proves centring and normalised vanishing. A Lean port is still a task; it is not the same task as discovering the ordinary implication.

## 3. Repairs to the old long assembly

| Assembly label or passage | Required disposition |
|---|---|
| `res:cubicexclusion` | Replace the universal `>=1/28` with positive lower exceptional density for each fixed rational profile. Its reductions assume density zero and use prime-dependent bounds. |
| `res:modseven`, `sec:modseven` | State the plus word at starts `0 mod 7` and the minus word at starts `1 mod 7`. Disjoint blocks give `1/7` only for the two terminal primitive profiles. Do not prohibit every four-step agreement at every phase. |
| `eq:shiftedsign` and its following sign claim | Retain the exact identity `gamma=-theta+Lambda`; use the strict/equal/reversed trichotomy. At equality the defect is zero, so the old weak-inequality sign assertion is not correct. |
| `res:recordamplified`, `res:criticalrate` | Do not promote local estimates to the old global equivalences without the required producer and overlap control. Use the live claim dispositions. |
| `res:epochenergy`, `res:energycriterion` | Preserve the finite protected-epoch inequality with its hypotheses. A global energy theorem needs existence and accounting of the epochs, not only the one-epoch inequality. |
| Integer covering windows | State integer endpoints and integer length, or explicitly insert the necessary rounding. |
| Rational-quadratic continuation after the integral-parameter proof | The sign of the negative norm does not transfer verbatim to rational `k`. At `k=1/2` the two displayed norms are positive (`161` and `97`). These values refute the sign argument, not the desired nonsquare conclusion. |
| Variable-rise extension | The old abstract counterexample has quantifiers `for every envelope, there exists a family and a walk`; the family depends on the envelope. Retain this order. Add the r6 exact coefficient-series realisation only after adjudication, with its non-unit and scale limitations. |

These repairs agree with the supplied `LONG_RECORD_DESTINATIONS.md`. The live cubic ring identities and finite-field orbit obstructions are present; the r4 rational-root-domain mismatch should not be re-reported as an unfixed defect in those newer files.

## 4. Where the cut text goes

`migrations/label_map.md` is the exact per-label crosswalk. The retained fragments divide the work by mathematical function:

- `algebra_and_examples.tex`: coordinate comparisons, scalar examples, and elementary variants.
- `reduced_tail_details.tex`: the longer reduced-state account, sparse-gcd bounds and finite-block consequences.
- `previous_transfer_and_assurance.tex`: the former transfer/source account, with the main proof already moved to the short note.
- `scalar_mass_versions.tex`: older mass formulations and examples; retain the strongest statement as the principal version.
- `frontier_and_formal_details.tex`: record/epoch catalogue, source guide and research programme.
- `short_note_appendices.tex`: complete constant/periodic, feedback, antecedent and certificate appendices.
- `formal_conjectures_reference.tex`: the uncited source-interface reference and its technical explanation, retained for the source guide.

Do not paste duplicate formulations into the companion indiscriminately. Retain one current theorem statement, attach older forms as consequences or historical failures, and keep the sharp counterexample next to the inference it falsifies. All old text is supplied so that the mathematical record is preserved during this integration.

No `02_long_record.tex` mutation, source promotion, barrel import, Comparator roster edit, or public submission was performed.
