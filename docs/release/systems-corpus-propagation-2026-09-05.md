# Systems paper and source-packet propagation

The current paper-frontier checkout now includes the accepted systems/strategy
changes from `2789f816ee`, `9352b0b8`, `aa167cbb`, `e6874e8e` and `e4864f4d`.
They landed together as `c3d88a69`, preserving the newer mathematical source
wave and the existing generated README inventory. The source-packet exporter,
human and agent routes, both authored papers, CI fixtures and research notes
are included.

The mathematical owner's #249 window-hypothesis correction `b4187d0e60` applied
cleanly as `ca12236652`. No mathematical proof was changed by this integration.

Both systems/strategy PDFs were rebuilt from the current checkout. The existing
navigation manuscript had changed generated coverage macros without a matching
PDF; its current source was rebuilt too. The paper corpus was regenerated by
its owning exporter, then the publication digests and entry packet refreshed.
No generated corpus was copied from the older systems checkout.

The corpus checker confirms all 30 recorded manuscript/PDF artifacts match
their hashes. Publication-contract and adversarial artifact checks pass.
Source-packet Git fixtures, clone-skill routing, architecture, systems-evidence,
writing and crosslink checks pass. The three rebuilt PDFs have 15 valid outgoing
cross-PDF destinations.

Rendered section checks now follow explanatory section boundaries rather than
requiring later paragraphs to stay on fixed page numbers. First-page requirements
remain bounded by pages. Tests reject missing or reversed section boundaries and
evidence placed outside its section; pagination changes alone are accepted.

This is publication propagation, not a new Lean replay or a full release verdict.
The new #257 transport work and its Comparator checks remain with their proof
owners. A further #249 sign-versus-certificate correction was announced by the
mathematical author while this pass ran; it needs its accepted commit integrated
in the next source refresh.
