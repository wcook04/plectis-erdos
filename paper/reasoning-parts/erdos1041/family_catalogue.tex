\clearpage
\section{Complete result-family map}\label{sec:erdos-1041-complete-family-map}

This section places every registered family for this problem in the shared 93-family reader order.  The five display bands control exposition only; the separate promotion state currently covers 23 families and is reported but does not hide or strengthen any family.  Mathematical statements and evidence modes come from the public claim registry.  Across all eight problems the public result-atom catalog contains 704 exact packet coordinates; this problem contributes 189.  Catalog rows expose bounded statement excerpts plus full-source digests, not a claim that every complete packet statement is reproduced here.

\subsection{Root retention}
\textbf{Reader position.} 7 of 93; display band: front door.  Formal editorial disposition: split.  These are separate classifications.

\textbf{Reader entry.} This family gathers the strongest geometric bridges, solved regimes, and sharp obstructions around the search for a short contained root-to-root path.

A quantified small constant perturbation keeps every polynomial root inside the open unit disc.

\textbf{Authority and reach.} Lean kernel; Comparator-selected; locally proved result; novelty unassessed.

\textbf{Exact boundary.} Root retention is one input to a still-incomplete route.

\textbf{Result-atom population.} 51 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} exact selected interface; 1 executable interface(s), 1 repository-registered selected result interface(s).

\textbf{Publication placement.} This family is also admitted to the short note.

\subsection{Newton value decay}
\textbf{Reader position.} 13 of 93; display band: major result.  Formal editorial disposition: split.  These are separate classifications.

\textbf{Reader entry.} Exact Newton transport supports a large collection of connector, capacity, allocation, and local-atlas results that must ultimately be split by mechanism.

The Newton value equation and its exponential first integral are checked exactly.

\textbf{Authority and reach.} Lean kernel; locally proved result and formalised calculus; novelty unassessed.

\textbf{Exact boundary.} Value decay alone does not give a short connecting curve.

\textbf{Result-atom population.} 95 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} not selected for comparator; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family is also admitted to the short note.

\begin{itemize}
  \item \nolinkurl{ErdosProblems.Erdos1041.newtonFlow_value_hasDerivAt}
  \item \nolinkurl{ErdosProblems.Erdos1041.newtonFlow_scaledValue_hasDerivAt_zero}
\end{itemize}

\subsection{Critical value separation connector}
\textbf{Reader position.} 24 of 93; display band: major result.  Formal editorial disposition: promote.  These are separate classifications.

\textbf{Reader entry.} For degree n >= 3, leading coefficient of modulus one, P(0)=1, P'(0)=0 and P''(0) != 0, assume every other critical point d has |1-P(d)| >= S > 1. The resolved branch P(Z(xi))=1-xi\textasciicircum{}2 extends injectively to |xi|<sqrt(S), and joins distinct roots with length at most 2(1+S)\textasciicircum{}(1/n) sqrt(log(S/(S-1))). It is shorter than two when (1+S)\textasciicircum{}(2/n) log(S/(S-1)) < 1. For a monic polynomial with roots in the open unit disk, the normalized separation regimes (S=4,n>=3), (S=3,n>=4), or (S=2,n>=6) yield an actual path shorter than two inside |f|<1. These regimes have S>=2, so the selected critical value has minimum modulus and the discriminant bound gives |f(c)|<1; for a general S>1 the scaled length estimate alone does not supply that containment. Mechanism: Resolve the simple saddle by the quadratic value coordinate. Proper unramified continuation and monodromy give a univalent inverse branch; the polynomial lemniscate area bound controls its coefficient energy, and Cauchy-Schwarz gives the explicit connector length. Critical-value separation selects a least-modulus value for the sublevel scaling.

For degree n >= 3, leading coefficient of modulus one, P(0)=1, P'(0)=0 and P''(0) != 0, assume every other critical point d has |1-P(d)| >= S > 1. The resolved branch P(Z(xi))=1-xi\textasciicircum{}2 extends injectively to |xi|<sqrt(S), and joins distinct roots with length at most 2(1+S)\textasciicircum{}(1/n) sqrt(log(S/(S-1))). It is shorter than two when (1+S)\textasciicircum{}(2/n) log(S/(S-1)) < 1. For a monic polynomial with roots in the open unit disk, the normalized separation regimes (S=4,n>=3), (S=3,n>=4), or (S=2,n>=6) yield an actual path shorter than two inside |f|<1. These regimes have S>=2, so the selected critical value has minimum modulus and the discriminant bound gives |f(c)|<1; for a general S>1 the scaled length estimate alone does not supply that containment. Mechanism: Resolve the simple saddle by the quadratic value coordinate. Proper unramified continuation and monodromy give a univalent inverse branch; the polynomial lemniscate area bound controls its coefficient energy, and Cauchy-Schwarz gives the explicit connector length. Critical-value separation selects a least-modulus value for the sublevel scaling.

\textbf{Authority and reach.} Ordinary full theorem; any separately identified Lean kernel has a narrower statement; ordinary proved restricted metric theorem; novelty unassessed.

\textbf{Exact boundary.} The full analytic continuation, polynomial area estimate and geometric assembly are ordinary mathematics. ErdosProblems/Erdos1041/FirstMergeCriticalValueSeparation.lean and the separate ExternalVerification1041FirstMergeCriticalValueSeparation package have been prepared for public promotion; focused public validation is pending. They concern numerical degree monotonicity, sufficient thresholds and a squared-length consumer, not analytic continuation or a polynomial path theorem. No terminal external Comparator receipt is asserted. Clustered critical values and multiple saddles are not covered; the criterion does not prove an unrestricted admissible-hub selector.

\textbf{Result-atom population.} 1 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} not applicable to comparator; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

\subsection{Sharp collinear diameter}
\textbf{Reader position.} 26 of 93; display band: major result.  Formal editorial disposition: promote.  These are separate classifications.

\textbf{Reader entry.} For a monic degree-n polynomial, n >= 2, with collinear zero occurrences of diameter D, two adjacent occurrences are connected by their segment of length at most D, on which |f| <= (D/2)\textasciicircum{}n / (2\textasciicircum{}(n-1) cos(pi/(2n))\textasciicircum{}n). The constant is optimal in every degree, with affine Chebyshev zero configurations attaining it. Open-unit-disk roots give a path shorter than two in |f|<1; coincident occurrences permit a constant path. Mechanism: Normalize the extreme roots to -1 and 1, take the alternating maxima in consecutive root gaps, and compare with a scaled Chebyshev polynomial by root counting. One gap has the sharp envelope; affine transport supplies the geometric segment and the Chebyshev configuration proves optimality.

For a monic degree-n polynomial, n >= 2, with collinear zero occurrences of diameter D, two adjacent occurrences are connected by their segment of length at most D, on which |f| <= (D/2)\textasciicircum{}n / (2\textasciicircum{}(n-1) cos(pi/(2n))\textasciicircum{}n). The constant is optimal in every degree, with affine Chebyshev zero configurations attaining it. Open-unit-disk roots give a path shorter than two in |f|<1; coincident occurrences permit a constant path. Mechanism: Normalize the extreme roots to -1 and 1, take the alternating maxima in consecutive root gaps, and compare with a scaled Chebyshev polynomial by root counting. One gap has the sharp envelope; affine transport supplies the geometric segment and the Chebyshev configuration proves optimality.

\textbf{Authority and reach.} Ordinary full theorem; any separately identified Lean kernel has a narrower statement; ordinary proved restricted metric theorem; novelty unassessed.

\textbf{Exact boundary.} SharpCollinear.existsPeakLeComparisonBound in ExternalVerification1041SolvedFamilies is a separately configured finite alternation kernel. Gap-maximizer existence, affine normalization, path transport and equality-node geometry are ordinary proof steps. Configuration and local checking do not constitute a terminal external Comparator receipt, and the original main Comparator roster does not represent this whole theorem.

\textbf{Result-atom population.} 1 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} not applicable to comparator; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

\subsection{Primitive sparse quintic paths}
\textbf{Reader position.} 27 of 93; display band: major result.  Formal editorial disposition: promote.  These are separate classifications.

\textbf{Reader entry.} For p(z)=z\textasciicircum{}5+a z\textasciicircum{}4+b z+c with all five zero occurrences w\_i in the closed unit disk, at least two distinct indices satisfy |b w\_i+c| <= 1. If a != 0, two satisfy strict inequality; if a=0 every index satisfies the bound, with equality exactly at |w\_i|=1. With open-unit-disk zeros the selected radial spokes produce a path shorter than two in |p|<1, with a constant path when selected occurrences coincide. Mechanism: Rotation and the missing coefficients give three Newton moment identities. A harmonic separator and a sum-of-squares bound rule out four unsafe tail energies. An Abel decomposition then turns two safe tails into entire safe radial spokes.

For p(z)=z\textasciicircum{}5+a z\textasciicircum{}4+b z+c with all five zero occurrences w\_i in the closed unit disk, at least two distinct indices satisfy |b w\_i+c| <= 1. If a != 0, two satisfy strict inequality; if a=0 every index satisfies the bound, with equality exactly at |w\_i|=1. With open-unit-disk zeros the selected radial spokes produce a path shorter than two in |p|<1, with a constant path when selected occurrences coincide. Mechanism: Rotation and the missing coefficients give three Newton moment identities. A harmonic separator and a sum-of-squares bound rule out four unsafe tail energies. An Abel decomposition then turns two safe tails into entire safe radial spokes.

\textbf{Authority and reach.} Ordinary full theorem; any separately identified Lean kernel has a narrower statement; ordinary proved restricted metric theorem; novelty unassessed.

\textbf{Exact boundary.} The separately configured primitiveQuintic\_twoStrictTailEnergies kernel assumes the finite moment equations; it does not derive them from the polynomial roots, perform the rotation, or assemble the Abel-spoke path. The full sparse-quintic theorem is ordinary mathematics, and no terminal external Comparator receipt for the separate package is asserted.

\textbf{Result-atom population.} 1 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} not applicable to comparator; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

\subsection{Translated cubic quotient fibres}
\textbf{Reader position.} 28 of 93; display band: major result.  Formal editorial disposition: promote.  These are separate classifications.

\textbf{Reader entry.} Let q >= 2, h be complex, P be monic cubic, and f(z)=P((z-h)\textasciicircum{}q). If all zeros of f lie in the open unit disk and f has at least two distinct zero values, two zeros are joined through h by a two-segment path shorter than two inside |f|<1. This solves this coefficient family in every degree 3q >= 6. Mechanism: Root-of-unity averaging puts every quotient root in the open unit disk. A cubic charge identity selects a root with a safe whole spoke by an AM-GM distance envelope. Two points of its cyclic fibre pull the safe spoke back to two segments through h; the zero-root fallback treats degeneracy.

Let q >= 2, h be complex, P be monic cubic, and f(z)=P((z-h)\textasciicircum{}q). If all zeros of f lie in the open unit disk and f has at least two distinct zero values, two zeros are joined through h by a two-segment path shorter than two inside |f|<1. This solves this coefficient family in every degree 3q >= 6. Mechanism: Root-of-unity averaging puts every quotient root in the open unit disk. A cubic charge identity selects a root with a safe whole spoke by an AM-GM distance envelope. Two points of its cyclic fibre pull the safe spoke back to two segments through h; the zero-root fallback treats degeneracy.

\textbf{Authority and reach.} Ordinary full metric theorem; Lean checks the actual cyclic-fibre mean square and quotient-disk bridge; ordinary proved restricted metric theorem; novelty unassessed.

\textbf{Exact boundary.} For a supplied primitive root of unity, Lean proves the complete orbit mean-square identity, the strict centre/radius budget, quotient-root disk membership, and composition-zero transfer. Constructing all required fibres, choosing or lifting the geometric path, handling degeneracies, and assembling its length bound remain ordinary proof steps. No full-theorem challenge replay is asserted.

\textbf{Result-atom population.} 1 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} not applicable to comparator; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

\begin{itemize}
  \item \nolinkurl{ErdosProblems.Erdos1041.cyclicFiber_sum_normSq}
  \item \nolinkurl{ErdosProblems.Erdos1041.cyclicFiber_normSq_add_lt_one}
  \item \nolinkurl{ErdosProblems.Erdos1041.cyclicFiber_quotient_norm_lt_one}
  \item \nolinkurl{ErdosProblems.Erdos1041.cyclicFiber_composition_zero}
\end{itemize}

\subsection{Translated quartic quotient fibres}
\textbf{Reader position.} 29 of 93; display band: major result.  Formal editorial disposition: promote.  These are separate classifications.

\textbf{Reader entry.} Let q >= 2, h be complex, P be monic quartic, and f(z)=P((z-h)\textasciicircum{}q). If all listed zeros of f lie in the open unit disk, two listed zero occurrences have a rectifiable connecting path shorter than two inside |f|<1. The ordinary argument lifts the cited quartic chord-or-radial theorem through every nontrivial cyclic power; multiplicity may give a degenerate path. Mechanism: Cyclic mean-square averaging puts the quotient roots in the open disk. For alpha=1/q, a lifted chord has density bounded by (sqrt(d\textasciicircum{}2+x\textasciicircum{}2))\textasciicircum{}(alpha-1) <= x\textasciicircum{}(alpha-1); integration bounds its length by |a|\textasciicircum{}alpha+|b|\textasciicircum{}alpha < 2. Splitting at the origin handles the branch singularity.

Let q >= 2, h be complex, P be monic quartic, and f(z)=P((z-h)\textasciicircum{}q). If all listed zeros of f lie in the open unit disk, two listed zero occurrences have a rectifiable connecting path shorter than two inside |f|<1. The ordinary argument lifts the cited quartic chord-or-radial theorem through every nontrivial cyclic power; multiplicity may give a degenerate path. Mechanism: Cyclic mean-square averaging puts the quotient roots in the open disk. For alpha=1/q, a lifted chord has density bounded by (sqrt(d\textasciicircum{}2+x\textasciicircum{}2))\textasciicircum{}(alpha-1) <= x\textasciicircum{}(alpha-1); integration bounds its length by |a|\textasciicircum{}alpha+|b|\textasciicircum{}alpha < 2. Splitting at the origin handles the branch singularity.

\textbf{Authority and reach.} Ordinary full metric theorem; Lean checks the actual cyclic-fibre mean square and quotient-disk bridge; ordinary proved restricted metric theorem; novelty unassessed.

\textbf{Exact boundary.} For a supplied primitive root of unity, Lean proves the complete orbit mean-square identity, the strict centre/radius budget, quotient-root disk membership, and composition-zero transfer. Constructing all required fibres, choosing or lifting the geometric path, handling degeneracies, and assembling its length bound remain ordinary proof steps. No full-theorem challenge replay is asserted.

\textbf{Result-atom population.} 1 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} not applicable to comparator; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

\begin{itemize}
  \item \nolinkurl{ErdosProblems.Erdos1041.cyclicFiber_sum_normSq}
  \item \nolinkurl{ErdosProblems.Erdos1041.cyclicFiber_normSq_add_lt_one}
  \item \nolinkurl{ErdosProblems.Erdos1041.cyclicFiber_quotient_norm_lt_one}
  \item \nolinkurl{ErdosProblems.Erdos1041.cyclicFiber_composition_zero}
\end{itemize}

\subsection{Critical value mean budget}
\textbf{Reader position.} 30 of 93; display band: major result.  Formal editorial disposition: promote.  These are separate classifications.

\textbf{Reader entry.} For monic f of degree 2 <= n <= 5 with roots in any radius-R disk, sum\_j |f(c\_j)|\textasciicircum{}(1/n) <= (n-1)R, counting critical multiplicities, sharply. In every degree FP\_(n-1) implies the budget: the reflected derivative and maximum modulus give |f(c\_j)| <= product\_k |1-conj(c\_j)c\_k| in the unit disk, radial contraction handles the boundary, and concavity supplies exponent 1/n. FP1-FP4 settle degrees two through five.

For monic f of degree 2 <= n <= 5 with roots in any radius-R disk, sum\_j |f(c\_j)|\textasciicircum{}(1/n) <= (n-1)R, counting critical multiplicities, sharply. In every degree FP\_(n-1) implies the budget: the reflected derivative and maximum modulus give |f(c\_j)| <= product\_k |1-conj(c\_j)c\_k| in the unit disk, radial contraction handles the boundary, and concavity supplies exponent 1/n. FP1-FP4 settle degrees two through five.

\textbf{Authority and reach.} Ordinary paper and research-note proofs; scalar Lean support is not a formalization of this theorem; ordinary proved auxiliary theorem; novelty unassessed.

\textbf{Exact boundary.} The reflected-derivative inequality is Lean-checked; the finite-point argument remains an ordinary proof, and Comparator replay of the reflected inequality is pending. The budget bounds critical values, not path lengths or hub selection. FP\_m remains open here for m >= 5; the radial family attains equality.

\textbf{Result-atom population.} 1 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} not applicable to comparator; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

\subsection{Low critical path certificate}
\textbf{Reader position.} 31 of 93; display band: major result.  Formal editorial disposition: promote.  These are separate classifications.

\textbf{Reader entry.} For any squarefree monic complex polynomial of degree at least two, minimum critical-value modulus at most 13/25 guarantees a rectifiable path of length below two between distinct roots inside its open unit lemniscate, with no root-location restriction. The proof combines hyperbolic root separation, a first-merger Green-function lower bound, and an area-growth contradiction; a portable exact rational certificate validates the threshold.

For any squarefree monic complex polynomial of degree at least two, minimum critical-value modulus at most 13/25 guarantees a rectifiable path of length below two between distinct roots inside its open unit lemniscate, with no root-location restriction. The proof combines hyperbolic root separation, a first-merger Green-function lower bound, and an area-growth contradiction; a portable exact rational certificate validates the threshold.

\textbf{Authority and reach.} Ordinary analytic proof plus exact rational computational certificate; ordinary restricted metric theorem with exact rational certificate.

\textbf{Exact boundary.} The sufficient small-critical-value condition is not proved for every unit-disk root configuration, so this does not solve the general problem. The analytic chain and certificate are not Lean formalized. Public source: research\_corpus/Erdos1041/LowCriticalPathCertificate.md; replay: experiments/erdos1041\_low\_critical\_path\_certificate.py.

\textbf{Result-atom population.} 1 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} not applicable to comparator; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

\subsection{Ray separation}
\textbf{Reader position.} 42 of 93; display band: mechanism.  Formal editorial disposition: split.  These are separate classifications.

\textbf{Reader entry.} Ray separation explains why certain Newton and straight-spoke connectors fail and identifies curvature, grouping, or compensation as the necessary alternatives.

Distinct positive rays exclude a finite Newton connection.

\textbf{Authority and reach.} Lean kernel; locally proved result; novelty unassessed.

\textbf{Exact boundary.} The result is a route obstruction, not the global theorem.

\textbf{Result-atom population.} 19 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} represented by selected interface; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family is also admitted to the short note.

\begin{itemize}
  \item \nolinkurl{ErdosProblems.Erdos1041.samePositiveRay_of_real_exp_decay}
  \item \nolinkurl{ErdosProblems.Erdos1041.no_newtonConnection_of_not_samePositiveRay}
\end{itemize}

\subsection{Published proof gap}
\textbf{Reader position.} 67 of 93; display band: frontier.  Formal editorial disposition: split.  These are separate classifications.

\textbf{Reader entry.} A local gap in a claimed proof is paired with exact component-capacity tools and a selector counterexample that clarify what a repair would have to overcome.

The paper identifies an invalid local saddle block in a claimed unrestricted proof.

\textbf{Authority and reach.} authored source analysis; paper diagnosis.

\textbf{Exact boundary.} The diagnosis does not prove that no repair exists.

\textbf{Result-atom population.} 4 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} not applicable to comparator; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family is also admitted to the short note.

\subsection{Lemniscate finite search}
\textbf{Reader position.} 75 of 93; display band: frontier.  Formal editorial disposition: split.  These are separate classifications.

\textbf{Reader entry.} Finite searches, exact counterexamples, and rigorous local connector charts map the boundary between failed global heuristics and provable subcases.

A bounded search records finite geometric evidence.

\textbf{Authority and reach.} external computation; finite computation.

\textbf{Exact boundary.} Finite evidence does not settle the universal problem.

\textbf{Result-atom population.} 6 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} not applicable to comparator; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

\subsection{Translation avoidance}
\textbf{Reader position.} 84 of 93; display band: technical support.  Formal editorial disposition: split.  These are separate classifications.

\textbf{Reader entry.} Small translations avoid finitely many collision lines, providing a clean genericity tool while later wall results retain their separate metric obligations.

Collision translations are parameterised by real affine lines and an arbitrarily small common translation avoids finitely many of them.

\textbf{Authority and reach.} Lean kernel; Comparator-selected; locally proved result; novelty unassessed.

\textbf{Exact boundary.} The finite-family avoidance theorem does not perform the global topology or metric gluing.

\textbf{Result-atom population.} 7 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} exact selected interface; 1 executable interface(s), 1 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

