\clearpage
\section{Complete result-family map}\label{long269:sec:erdos-269-complete-family-map}

This section places every registered family for this problem in the shared 93-family reader order.  The five display bands control exposition only; the separate promotion state currently covers 23 families and is reported but does not hide or strengthen any family.  Mathematical statements and evidence modes come from the public claim registry.  Across all eight problems the public result-atom catalog contains 704 exact packet coordinates; this problem contributes 65.  Catalog rows expose bounded statement excerpts plus full-source digests, not a claim that every complete packet statement is reproduced here.

\subsection{Three prime lcm cells}
\textbf{Reader position.} 6 of 93; display band: front door.  Formal editorial disposition: merge.  These are separate classifications.

\textbf{Reader entry.} The three-prime running LCM is constant on logarithmic cells and changes by exact prime-power jumps, giving the natural coordinate system for Erdős 269.

Exact running-LCM product, logarithmic-cell constancy, coordinate jump ratios, and jump count.

\textbf{Authority and reach.} Lean kernel; Comparator-selected; locally proved result; novelty unassessed.

\textbf{Exact boundary.} Cell structure alone does not prove irrationality.

\textbf{Result-atom population.} 5 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} exact selected interface; 1 executable interface(s), 1 repository-registered selected result interface(s).

\textbf{Publication placement.} This family is also admitted to the short note.

\subsection{Two prime transcendence}
\textbf{Reader position.} 17 of 93; display band: major result.  Formal editorial disposition: hold.  These are separate classifications.

\textbf{Reader entry.} The two-prime analogues are transcendental, showing that the unresolved difficulty first appears when the third prime channel is added.

Both two-prime running-lcm series are proved transcendental by an authored deduction from an external theorem.

\textbf{Authority and reach.} paper argument plus cited theorem; paper plus external theorem.

\textbf{Exact boundary.} Comparator cannot certify the external analytic input.

\textbf{Result-atom population.} 1 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} not applicable to comparator; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family is also admitted to the short note.

\subsection{Weighted phase carry observer}
\textbf{Reader position.} 41 of 93; display band: mechanism.  Formal editorial disposition: hold.  These are separate classifications.

\textbf{Reader entry.} Weighted phase coordinates expose a finite residue digit but also an uncontrolled integral coboundary, precisely marking what a faithful finite-dimensional observer must retain.

An exact weighted-phase carry recurrence splits into a finite residue digit and an uncontrolled integral quotient coboundary; an explicit function-faithful finite-dimensional observer then forces finite realised span.

\textbf{Authority and reach.} Lean kernel; locally proved result; novelty unassessed.

\textbf{Exact boundary.} The recurrence supplies a finite residue coordinate but leaves an uncontrolled integral quotient coboundary; it proves neither a finite-state quotient nor a literal infinite realised span. Finite realised span requires an explicit factorisation through a finite-dimensional function-faithful observer, and scalar evaluation alone is insufficient. The actual \{2,3,5\} series-to-carry bridge is now proved separately in the paper, not by this observer theorem and not yet in Lean. Cofinal residue escape and the \#269 endpoint remain open.

\textbf{Result-atom population.} 7 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} exact selected interface; 1 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

\begin{itemize}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.\allowbreak carry\_\allowbreak eq\_\allowbreak residueDigit\_\allowbreak add\_\allowbreak coboundary}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.\allowbreak carryResidue\_\allowbreak mem\_\allowbreak interval}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.\allowbreak residueDigit\_\allowbreak mem\_\allowbreak interval}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.\allowbreak finite\_\allowbreak realisedSpan\_\allowbreak of\_\allowbreak factorisation}
\end{itemize}

\subsection{Height fibre and shell}
\textbf{Reader position.} 46 of 93; display band: mechanism.  Formal editorial disposition: merge.  These are separate classifications.

\textbf{Reader entry.} Height fibres and smooth shells organize many LCM cells into exact finite normal forms with controlled multiplicity.

Finite height-fibre normalization and the quadratic shell multiplicity bound are formalised. The paper sums the literal shells and proves X\_a <= (n0\textasciicircum{}2+8n0+18)/9, where n0 is the sum of the floor logarithms of 2\textasciicircum{}a in bases 2,3,5. This gives the existing integer short bound K235(B,a) for an integral reduced carry B X\_a.

\textbf{Authority and reach.} Lean kernel for finite fibre and shell bounds; ordinary paper proof for the actual infinite shell sum and its quantitative normalized-tail bound; locally proved result; novelty unassessed.

\textbf{Exact boundary.} The infinite shell construction, series identification and tail bound are not yet formalised by the linked finite-source declarations. The actual cofinal residue-window escape remains unproved; the bound alone is no irrationality result.

\textbf{Result-atom population.} 5 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} represented by selected interface; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

\begin{itemize}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.\allowbreak finiteSmoothKernelSum\_\allowbreak groupedByHeight}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.\allowbreak smoothExponentShell\_\allowbreak card\_\allowbreak quadratic}
\end{itemize}

\subsection{Conditional carry escape}
\textbf{Reader position.} 63 of 93; display band: frontier.  Formal editorial disposition: hold.  These are separate classifications.

\textbf{Reader entry.} A cofinal local-window residue escape would kill every positive reduced carry; proving that escape for the actual three-prime series is the central open step.

The paper now derives the literal repeated \{2,3,5\} running-LCM carry from a hypothetical rational value: exact shell-digit transport gives the recurrence, a finite cleared prefix removes the denominator smooth part after a denominator-dependent onset, and X\_a <= (n0\textasciicircum{}2+8n0+18)/9 gives the tested integer bound K235(B,a). Thus actual cofinal local-window residue escape is the remaining arithmetic input for this irrationality route. The existing Lean/Comparator consumer excludes any positive integral carry satisfying its recurrence, bound and escape predicate.

\textbf{Authority and reach.} Ordinary paper proof for the actual infinite-series bridge, eventual smooth-factor cancellation and carry bound; Lean kernel; Comparator-selected for the already configured abstract finite-window consumer; conditional actual-series reduction with a formalised finite consumer; novelty unassessed.

\textbf{Exact boundary.} The formal predicate is CofinalLocalWindowEscape. For the literal \{2,3,5\} radix/digit data, the surviving producer must supply, for every positive B coprime to 30 and every onset a0>=1, an index ell>=a0 and length h>=1 whose least positive residue of -B times the window forcing modulo the window-base product exceeds K235(B,ell+h). This cofinal escape is unproved. The actual series bridge, smooth cancellation and bound are ordinary mathematical arguments, not new Comparator evidence or completed Lean formalizations. The existing source theorem still assumes the exact recurrence, positive integral carry and short bound. Irrationality remains open; this does not settle arbitrary three-prime sets or the de-duplicated series.

\textbf{Result-atom population.} 37 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} exact selected interface; 1 executable interface(s), 1 repository-registered selected result interface(s).

\textbf{Publication placement.} This family is also admitted to the short note.

\begin{itemize}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.\allowbreak CofinalLocalWindowEscape}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.}\\\texttt{no\_\allowbreak positive\_\allowbreak reducedCarry\_\allowbreak of\_\allowbreak cofinalLocalWindowEscape}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.\allowbreak windowBase}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.\allowbreak windowForcing}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.\allowbreak leastPositiveResidue}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.}\\\texttt{no\_\allowbreak positive\_\allowbreak absorbedCarry\_\allowbreak of\_\allowbreak cofinalLocalWindowEscape}
\end{itemize}

\subsection{Rank two kernel no go}
\textbf{Reader position.} 74 of 93; display band: frontier.  Formal editorial disposition: hold.  These are separate classifications.

\textbf{Reader entry.} The 2,3,5 kernel has genuine rank-two structure, so any proof that treats the prime channels as a single separable mode is invalid.

The 2,3,5 kernel has checked minor -1/15 and is not rank one. The paper additionally proves arbitrarily large nonsingular minors for three distinct primes by a dense-rotation staircase construction, excluding every finite exact separation across the displayed coordinate split.

\textbf{Authority and reach.} Lean kernel; Comparator-selected for the specified 2-by-2 minor and rank-one obstruction; ordinary paper proof for arbitrary-order non-separability; no-go result.

\textbf{Exact boundary.} The arbitrary-order dense-rotation and determinant argument is not covered by the existing Comparator representative or a linked Lean theorem. Neither finite-rank obstruction establishes irrationality or transcendence.

\textbf{Result-atom population.} 3 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} exact selected interface; 1 executable interface(s), 1 repository-registered selected result interface(s).

\textbf{Publication placement.} This family is also admitted to the short note.

\subsection{Dyadic block alphabet}
\textbf{Reader position.} 85 of 93; display band: technical support.  Formal editorial disposition: merge.  These are separate classifications.

\textbf{Reader entry.} Every dyadic LCM block uses one of four exact radix values, sharply reducing the local combinatorial possibilities.

The exact dyadic block alphabet is 2, 6, 10, and 30.

\textbf{Authority and reach.} Lean kernel; locally proved result; novelty unassessed.

\textbf{Exact boundary.} The four-letter alphabet is formalised. Its identification with the literal infinite shell recurrence and ordered digit is proved in the paper but not yet in Lean; the alphabet alone does not supply actual carry escape.

\textbf{Result-atom population.} 5 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} represented by selected interface; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

\begin{itemize}
  \item \texttt{ErdosProblems.\allowbreak Erdos269.\allowbreak dyadicBlockBase235\_\allowbreak cases}
\end{itemize}

\subsection{Three prime finite search}
\textbf{Reader position.} 87 of 93; display band: technical support.  Formal editorial disposition: hold.  These are separate classifications.

\textbf{Reader entry.} A bounded exact search finds local residue certificates, but it cannot establish the cofinal escape required by the carry argument.

A local-window checker searches 106666 denominator and start pairs and records small certificates.

\textbf{Authority and reach.} external exact computation; finite computation.

\textbf{Exact boundary.} Finite search is not a cofinal statement.

\textbf{Result-atom population.} 2 of 704 public coordinates. The public atom catalog groups them under this family.

\textbf{Comparator assurance.} not applicable to comparator; 0 executable interface(s), 0 repository-registered selected result interface(s).

\textbf{Publication placement.} This family remains in the complete long record and is not a short-note headline.

